[
  {
    "application_id": "hrms-service",
    "integration_name": "HRMS authoritative identity feed",
    "connector_type": "Delimited/JSON authoritative source",
    "authentication_method": "Local service trust / file import for lab",
    "aggregation_method": "Scheduled authoritative identity import",
    "provisioning_method": "Not applicable - HRMS is source of truth",
    "correlation_rule": "employee_id",
    "account_schema": ["employee_id", "lan_id", "email", "display_name", "department", "manager_id", "identity_status"],
    "entitlement_schema": [],
    "supported_operations": ["read identities", "detect joiner", "detect mover", "detect leaver"],
    "market_pattern": "Workday/SAP SuccessFactors/Oracle HCM authoritative source",
    "standard_controls": ["authoritative lifecycle source", "leaver detection", "manager relationship", "department ownership"]
  },
  {
    "application_id": "idp-service",
    "integration_name": "IdP/OIDC identity and app registration integration",
    "connector_type": "OIDC/OAuth demo + directory graph model",
    "authentication_method": "Authorization code flow demo",
    "aggregation_method": "Directory users, groups, app registrations, OAuth clients",
    "provisioning_method": "Group/app assignment model",
    "correlation_rule": "employee_id then lan_id then email",
    "account_schema": ["sub", "preferred_username", "email", "role", "identity_status"],
    "entitlement_schema": ["groups", "app roles", "OAuth clients", "enterprise app assignments"],
    "supported_operations": ["authenticate", "issue claims", "list users", "list groups", "list app registrations", "list OAuth clients"],
    "market_pattern": "Microsoft Entra ID / Okta workforce identity provider",
    "standard_controls": ["SSO", "app registration", "enterprise applications", "role claims", "conditional access placeholder", "sign-in/audit visibility"]
  },
  {
    "application_id": "iga-service",
    "integration_name": "IGA governance application",
    "connector_type": "OIDC relying party + normalized governance repository",
    "authentication_method": "IdP-backed OIDC demo login",
    "aggregation_method": "Normalized account, entitlement, assignment, correlation datasets",
    "provisioning_method": "Governance simulation only",
    "correlation_rule": "identity_id mapped from account correlation results",
    "account_schema": ["identity_id", "account_id", "source", "entitlement", "assignment_status"],
    "entitlement_schema": ["risk_level", "status", "description", "source"],
    "supported_operations": ["view identities", "view sources", "view accounts", "view entitlements", "detect orphan accounts", "detect high-risk access"],
    "market_pattern": "SailPoint IdentityIQ / Identity Security Cloud governance console",
    "standard_controls": ["RBAC", "certification placeholder", "policy violation placeholder", "orphan detection", "high-risk access review"]
  },
  {
    "application_id": "jdbc-target",
    "integration_name": "JDBC legacy target aggregation",
    "connector_type": "JDBC / SQL view",
    "authentication_method": "Database credential / service account in real implementation",
    "aggregation_method": "SQL queries over users, roles, and mappings",
    "provisioning_method": "SQL insert/update/delete simulation later",
    "correlation_rule": "employee_id",
    "account_schema": ["native_account_id", "employee_id", "lan_id", "email", "account_status"],
    "entitlement_schema": ["role_id", "role_name", "risk_level", "description"],
    "supported_operations": ["aggregate accounts", "aggregate entitlements", "aggregate assignments", "detect orphan accounts", "detect privileged roles"],
    "market_pattern": "SailPoint JDBC connector pattern",
    "standard_controls": ["read-only aggregation", "service account", "least privilege SQL access", "correlation validation"]
  },
  {
    "application_id": "zsp-jit-app",
    "integration_name": "OIDC JIT enterprise SaaS integration",
    "connector_type": "OIDC relying party + app-local RBAC",
    "authentication_method": "IdP authorization code flow demo",
    "aggregation_method": "IGA normalized view of JIT-created local users and app roles",
    "provisioning_method": "JIT user creation on first login; temporary access request for elevation",
    "correlation_rule": "employee_id from IdP claim mapped to IGA identity",
    "account_schema": ["user_id", "username", "display_name", "email", "idp_role", "app_role", "last_login_at"],
    "entitlement_schema": ["ZSP_VIEWER", "ZSP_OPERATOR", "ZSP_APPROVER", "ZSP_ADMIN", "ZSP_SESSION_ADMIN"],
    "supported_operations": ["OIDC login", "JIT provision user", "map role claim", "request temporary privilege", "record audit event"],
    "market_pattern": "Modern SaaS with Entra/Okta SSO, JIT provisioning, and zero standing privilege",
    "standard_controls": ["no standing admin by default", "time-bound elevation", "audit events", "role mapping", "review critical temporary access"]
  },
  {
    "application_id": "scim-target",
    "integration_name": "SCIM SaaS provisioning target",
    "connector_type": "SCIM 2.0",
    "authentication_method": "Bearer token / OAuth client credential in real implementation",
    "aggregation_method": "GET /Users and GET /Groups",
    "provisioning_method": "POST/PATCH/DELETE /Users and PATCH /Groups",
    "correlation_rule": "userName or externalId",
    "account_schema": ["id", "userName", "externalId", "active", "emails", "displayName"],
    "entitlement_schema": ["Group.id", "Group.displayName", "members"],
    "supported_operations": ["create user", "update user", "disable user", "read groups", "update group membership"],
    "market_pattern": "SaaS SCIM 2.0 connector",
    "standard_controls": ["standard schema", "automated provisioning", "automated deprovisioning", "group membership management"]
  }
]
